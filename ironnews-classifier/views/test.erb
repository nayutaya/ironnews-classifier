
<%
  all_documents = BayesOneDocument.all(
    :trained => false,
    :limit   => 100)
  target_documents = all_documents.sort_by { rand }[0, 1]

  target_documents.each { |document|
    #document.trained = true
    #document.save
  }
%>

<%=h target_documents.inspect %>
